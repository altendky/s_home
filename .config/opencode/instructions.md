# Temporary Files

Use `/tmp/agents/<uuid>/` as your temporary directory. Generate a UUID and create this directory on first need, then reuse the same path for the remainder of the session. This path has pre-approved allow permissions so no permission prompts are needed.
