[env]
# Skip expensive Rust compilation hooks in pre-commit/pre-push.
# Run `just check-rust` and `just test-rust` manually instead.
SKIP = "rust-clippy,rust-test"
OPENCODE_BINARY = "{{env.HOME}}/.local/share/mise/installs/opencode/1.3.17/opencode"
